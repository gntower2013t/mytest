import { browser, by, element, $, $$ } from 'protractor';


browser.sleep(1000);
browser.driver.sleep(1000);

elements
// .each(ele => ele.getText().then(v => console.log("ele: " + v)))
// .all(by.linkText(name)).first();
